# Twin Plan — Criação de Usuários Admin com Permissões

**Selected Specialist:** developer (fullstack)
**Selection Reason:** user_override (opção 3 escolhida pelo usuário)
**Timestamp:** 2026-05-13
**Quality Level:** pragmatic

---

## Contexto

O sistema atual possui 4 perfis (comprador, corretor, imobiliaria, admin) controlados por um campo `perfil` string no model `Usuario`. Não existe sistema de permissões granulares. O admin pode editar usuários mas não criar novos com papéis específicos e permissões customizadas.

## Objetivo

Permitir que admins criem novos usuários (qualquer perfil, incluindo admin) com definição de permissões granulares personalizadas por usuário.

## Estratégia

Abordagem pragmática: adicionar campo `permissoes` (JSON array) ao model `Usuario`, definir enum de permissões disponíveis, criar endpoint de criação admin e UI correspondente.

---

## Etapas de Implementação

### ETAPA 1 — Backend: Modelo de Permissões

**Arquivo:** `backend/app/models/usuario.py`
- Adicionar campo `permissoes` do tipo `JSON`, nullable, default `[]`
- Documentar com as permissões disponíveis

**Arquivo:** `backend/app/schemas/usuario.py`  
- Adicionar enum `PermissaoEnum` com as permissões:
  - `gerenciar_usuarios` — criar/editar/desativar usuários
  - `gerenciar_imoveis` — aprovar/rejeitar/editar imóveis
  - `gerenciar_leads` — visualizar e redistribuir leads
  - `gerenciar_planos` — alterar planos e datas de expiração
  - `ver_relatorios` — acesso a stats e relatórios
  - `gerenciar_cupons` — criar/editar cupons
  - `configuracoes_sistema` — configurações gerais do sistema
- Criar schemas `AdminCriarUsuario` (request) e `UsuarioAdminResponse` (response com permissoes)

### ETAPA 2 — Backend: Alembic Migration

**Arquivo:** `backend/alembic/versions/[hash]_add_permissoes_usuario.py`
- Migration para adicionar coluna `permissoes` JSON na tabela `usuarios`

### ETAPA 3 — Backend: Endpoint de Criação

**Arquivo:** `backend/app/routers/admin.py`
- Adicionar `POST /admin/users` — cria usuário com perfil e permissões definidos pelo admin
- Campos: nome, email, senha, perfil, permissoes (list)
- Validar email único, hash de senha, aplicar permissões

**Arquivo:** `backend/app/services/admin_service.py`
- Adicionar função `criar_usuario_admin(db, dados, admin_atual)`

**Arquivo:** `backend/app/schemas/admin.py`
- Adicionar `AdminCriarUsuarioRequest` e atualizar `UsuarioAdminResponse`

### ETAPA 4 — Backend: Atualizar PATCH de Usuário

**Arquivo:** `backend/app/routers/admin.py` — endpoint `PATCH /admin/users/{user_id}`
- Incluir `permissoes` como campo editável

**Arquivo:** `backend/app/schemas/admin.py` — `AdminAtualizarUsuario`
- Adicionar campo `permissoes: Optional[List[PermissaoEnum]]`

### ETAPA 5 — Frontend: Modal de Criação de Usuário

**Arquivo:** `frontend/src/app/dashboard/admin/users/page.tsx`
- Adicionar botão "Novo Usuário" no header da página
- Criar `ModalCriarUsuario` (pode ser no mesmo arquivo):
  - Campos: nome, email, senha, confirmar senha, perfil (select), permissões (checkboxes por grupo)
  - Submit para `POST /api/v1/admin/users`
  - Toast de sucesso/erro
- Atualizar `ModalEditarUsuario` para incluir permissões como checkboxes

### ETAPA 6 — Frontend: Exibição de Permissões na Tabela

**Arquivo:** `frontend/src/app/dashboard/admin/users/page.tsx`
- Adicionar coluna/tooltip de permissões na tabela de usuários (ícone de escudo com count)

---

## Ordem de Execução

1. `backend/app/models/usuario.py` — adicionar campo permissoes
2. `backend/app/schemas/usuario.py` — adicionar PermissaoEnum
3. `backend/app/schemas/admin.py` — novos schemas
4. `backend/app/services/admin_service.py` — função criar_usuario_admin
5. `backend/app/routers/admin.py` — POST /admin/users + PATCH atualizado
6. Alembic migration
7. `frontend/src/app/dashboard/admin/users/page.tsx` — UI completa

---

## QA / Validação

- [ ] `POST /admin/users` retorna 201 com usuário criado
- [ ] Email duplicado retorna 409
- [ ] Usuário criado aparece na listagem
- [ ] Permissões salvas corretamente no banco
- [ ] `PATCH /admin/users/{id}` atualiza permissões
- [ ] Non-admin não acessa endpoints admin (401/403)
- [ ] Frontend: modal abre e fecha corretamente
- [ ] Frontend: checkboxes de permissões funcionam
- [ ] Frontend: tabela exibe badge de permissões

---

## Arquivos a Modificar

| Arquivo | Ação |
|---|---|
| `backend/app/models/usuario.py` | Adicionar campo `permissoes` |
| `backend/app/schemas/usuario.py` | Adicionar `PermissaoEnum` |
| `backend/app/schemas/admin.py` | Novos schemas de criação/resposta |
| `backend/app/services/admin_service.py` | Função `criar_usuario_admin` |
| `backend/app/routers/admin.py` | POST + PATCH atualizados |
| `backend/alembic/versions/[novo].py` | Migration |
| `frontend/src/app/dashboard/admin/users/page.tsx` | UI completa |
